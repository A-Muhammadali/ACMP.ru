#include<bits/stdc++.h>
using namespace std;
int main() {
	/*
	long long int a,b,c,x,y,z,k,h,l,m,n,o;
	cin >>a>>b>>c>>x>>y>>z;
	k=max(max(a,b),c);
	n=max(max(x,y),z);
	l=min(min(a,b),c);
	h=min(min(x,y),z);
	o=(a+b+c-k-l)*(x+y+z-n-h);
	m=k*n+l*h+o;
	cout<<m;
	*/
	/*
	long long int a,b,c,x,y,z,m;
	cin >>a>>b>>c;
	x=a/2;
	y=b/6;
	z=c/1;
	m=min(min(x,y),z);
	cout<<m;
	*/

	/*
	long long int a,b,c,x,y,z,k,h,l,m,n,o;
	cin >>a>>b>>c>>x>>y>>z;
	k=max(max(a,b),c);
	n=max(max(x,y),z);
	l=min(min(a,b),c);
	h=min(min(x,y),z);
	o=(a+b+c-k-l)*(x+y+z-n-h);
	m=k*n+l*h+o;
	cout<<m;
	*/
	/*
	int a;
	cin >>a;
	if((1000<=a<=9999) and(a/1000==a%10)and(a/100%10==a%100/10)) {
		cout<<"Yes";
	} 
	else 
	{
		cout<<"No";
	}
	int,a;
	cin>>a;
	if (a=) 
	*/
	int a,b,c;
	b=2/3;
	a=5-3/4+3/4;
	c=21+1/1-1/1;
	cout<<"1-quymadan"<<" "<<a<<" "<<"ulush"<<" "<<"2-quymadan"<<" "<<c<<" "<<"ulush";
	
}
